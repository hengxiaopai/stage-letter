# PRODUCT.md — 产品愿景与定位

## 一句话定义

> **开场信 = "我关注的人开播了，第一时间告诉我。"**

只这一件事。

## 产品定位

我们不是"直播聚合器"，不是"直播录制器"，不是"直播播放器"。

我们是 **跨平台主播订阅通知服务**：

- 用户在微信小程序里关注一个主播（任意公开平台）
- 服务端 7×24 监控这些主播
- 主播开播 → 微信服务消息触达用户

> 内部暂定产品名：**开场信 / StageLetter**。  
> 后续如需更名，所有文档统一替换。

## 目标用户

### 核心用户（C 端）

**有跨平台关注习惯的重度直播观众**

特征：
- 同时关注 5+ 个主播
- 主播分布在不同平台（抖音、B 站、虎牙、斗鱼、Twitch…）
- 不想错过任何一个开播
- 不想装浏览器扩展、不愿意电脑常驻
- 主力使用场景是通勤、午休、晚上

### 次要用户（V1 不专门服务）

- 主播团队 / MCN：关注竞品主播开播节奏
- 内容运营：关注行业 KOL 动态
- 直播带货买手：跟踪特定主播开播时间

V1 不会为这些场景做专门设计，但架构上不排斥后续扩展。

## 核心场景

### 场景一：添加订阅

```
用户打开「开场信」小程序
   ↓
点击首页「添加主播」
   ↓
粘贴抖音链接：https://v.douyin.com/abc123
   ↓
客户端校验：域名在支持列表
   ↓
POST /api/v1/anchors/parse
   ↓
服务端解析 → 返回：
  平台：抖音
  主播：小杨哥
  头像：xxx
   ↓
UI 展示主播卡片
   ↓
用户点击「添加订阅」→ POST /api/v1/subscriptions
   ↓
成功，列入「我的订阅」
```

### 场景二:开播通知 (v0.2 重写)

```
时间 20:31
   ↓
服务端 Probe Worker 检测到「小杨哥」开播
   ↓
状态机:OFFLINE → SUSPECT_ONLINE → ONLINE(2 次确认)
   ↓
写 LiveSession(OPEN) + LiveEvent(CONFIRMED_ONLINE)
   ↓
Notification Service:查询关注「小杨哥」的所有 user
   ↓
找到 100 个 user
   ↓
对每个 user 决策:
  - notify_enabled = false → 跳过
  - available_grant > 0 → 尝试 wechat
    ├─ 成功 → consumed + 1
    ├─ 4xx(用户拒收)→ grant 失效
    └─ 5xx / 网络 → 重试,grant 保留
  - available_grant = 0 → 站内消息(reason='no_grant')
   ↓
写 notification_jobs + notification_deliveries(UNIQUE 去重)
   ↓
Worker 投递
   ↓
用户手机响起推送(部分 user 收到微信,部分收到站内)
```

### 场景三:开启更多提醒 (v0.2 改名,语义修正)

> **v0.1 错误**: 名为"补充提醒次数"+8。  
> **v0.2 修正**: 名为"开启更多提醒"+1(每次主动授权只产生一次机会)。

```
张三发现 grant available = 0
   ↓
进入「我的」页面
   ↓
点击「开启更多提醒」
   ↓
小程序调 wx.requestSubscribeMessage({ tmplIds: [...] })
   ↓
弹出微信授权
   ↓
用户同意(可勾选「总是保持以上选择」)
   ↓
客户端收到 accept → POST /api/v1/notifications/request-grant
   ↓
服务端:wechat_subscription_grants.granted_count += 1
   ↓
返回新额度 → UI 更新
```

### 场景四：查看主播详情

```
微信通知到达
   ↓
用户点击通知
   ↓
跳转到小程序 pages/anchor/detail?id=xxx
   ↓
详情页显示：
  - 主播头像、昵称
  - 平台、开播时间
  - 直播标题、封面
  - 观看人数
  - [前往观看]（走 PlatformDeepLinkAdapter）
```

## 产品原则

### 1. 极简

V1 只做一件事：**关注主播 → 开播通知**。  
任何与这条主线无关的功能都进入 [NON-GOALS.md](./NON-GOALS.md)。

### 2. 透明

用户必须能清楚看到:
- 我关注了谁
- 谁开播了
- 我现在有多少 grant 可用(不是"剩余配额",是"我授权过的次数")
- 为什么某次通知没送到(grant 用尽 / 平台关闭 / 通知被微信退订 / 平台 DEGRADED 低 confidence)

### 3. 可信

不能"主播上线/下线"反复抖，导致用户收到 10 条一样的通知。
- 状态机二次确认（见 [ARCHITECTURE.md §5.3](./ARCHITECTURE.md)）
- LiveSession + UNIQUE 约束去重
- 平台故障必须隔离，不能拖垮全局

### 4. 不打扰

- 单主播通知开关
- 微信 grant 自然限制(用户不愿频繁授权 → 自然减少通知)
- 后续 V2 加静默时间段

### 5. 不承诺做不到的事 (v0.2 加强)

- **不承诺"永久 Push"**:微信订阅消息本质是一次性授权,v0.2 不再伪造"季度重置 8 次"
- **不承诺"统一 3min 通知"**:SLA 按平台分级(Twitch < 30s,其他 < 5-8min),详见 [PRD §N1](./PRD.md)
- **不承诺所有平台都支持**:每个平台有显式支持状态
- **诚实标注低 confidence**:DEGRADED 平台产生的事件明确告知用户"平台不稳定,可能误报"

## 反愿景（Anti-Vision）

我们明确**不是**：

- ❌ 直播录制工具
- ❌ 弹幕 / 评论客户端
- ❌ 直播在线播放器
- ❌ 主播搜索 / 推荐
- ❌ 社交 / 聊天 / 关注用户
- ❌ AI 智能推荐

详见 [NON-GOALS.md](./NON-GOALS.md)。

## 长期演进

V1 是"开播通知器"。  
长期可以演进为 **创作者订阅情报中心（Creator Follow Intelligence）**：

```
V1: 主播开播提醒
   ↓
V2: 主播动态提醒（新视频、动态、专栏）
   ↓
V3: 直播预约
   ↓
V4: 跨平台主播动态时间线
   ↓
V5: 主播情报雷达（开播节奏、热度、跨平台身份合并）
```

但这些**都不是 V1 的事**。  
V1 必须先把"通知送得到"做扎实，再考虑扩展。

## 产品名字暂定

- 中文：**开场信**
- 英文：**StageLetter**
- 小程序名：`开场信`

> 名字属于可改的运营层资产，技术立项先用 StageLetter 这个内部代号。
