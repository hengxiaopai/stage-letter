<scroll-view scroll-x enable-flex class="ft" show-scrollbar="{{false}}">
  <view class="ft-item {{active === item.value ? 'ft-on' : ''}}" wx:for="{{items}}" wx:key="value" data-value="{{item.value}}" bindtap="onTap">{{item.label}}</view>
</scroll-view>
